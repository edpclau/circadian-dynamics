% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_actogram2.R
\name{plot_actogram2}
\alias{plot_actogram2}
\title{Plot Actograms (alternative workflow)}
\usage{
plot_actogram2(df)
}
\arguments{
\item{df}{The \code{data} tibble from the list returned by \code{simplify_data()}, containing
columns \code{datetime}, \code{raw_values}, and optionally \code{window} and \code{ld}.}
}
\value{
A named list of \code{ggplot} objects, one per individual/data series, each showing a
  double-plotted actogram faceted by day.
}
\description{
Produces double-plotted (48-hour) actograms from the \code{data} element returned by
  \code{\link{simplify_data}}. This function overlaps in purpose with
  \code{\link{plot_actogram_windows}} but consumes the simplified nested output rather than the
  raw windowed data.frame, and is not yet wired into the default \code{process_timeseries} workflow.
}
