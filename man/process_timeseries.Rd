% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{process_timeseries}
\alias{process_timeseries}
\alias{process_timeseries.rmv_gaps}
\alias{process_timeseries.na_to_zero}
\alias{process_timeseries.waveform}
\alias{process_timeseries.core}
\alias{process_timeseries.main}
\title{Helper function to prepare raw data for analysis}
\usage{
process_timeseries.rmv_gaps(df = NULL, sampling_rate = NULL)

process_timeseries.na_to_zero(df = NULL)

process_timeseries.waveform(
  df = NULL,
  detrend_data = TRUE,
  smooth_data = FALSE,
  butterworth = TRUE,
  f_low = 1/4,
  f_high = 1/73,
  order = 2,
  binning_n = 4
)

process_timeseries.core(
  df = NULL,
  make_windows = FALSE,
  window_size_in_days = 3,
  window_step_in_days = 1,
  sampling_rate = "1 hour",
  detrend_data = TRUE,
  smooth_data = FALSE,
  butterworth = TRUE,
  from = 18,
  to = 30,
  f_low = 1/4,
  f_high = 1/73,
  order = 2,
  big_data = FALSE,
  ofac = 1,
  lomb_pvalue = 0.01
)

process_timeseries.main(
  df = NULL,
  make_windows = FALSE,
  window_size_in_days = 3,
  window_step_in_days = 1,
  from = 18,
  to = 30,
  sampling_rate = "1 hour",
  detrend_data = TRUE,
  movavg = FALSE,
  butterworth = TRUE,
  f_low = 1/4,
  f_high = 1/73,
  order = 2,
  big_data = FALSE,
  ofac = 10,
  lomb_pvalue = 0.01
)
}
\arguments{
\item{df}{A data.frame where the first column is a POSIXct object and the rest are independent measurement values.}

\item{sampling_rate}{A character string indicating the sampling rate of the data. Examples: '30 minutes', '1 hour', '4 seconds', '100 days'.}

\item{detrend_data}{Logical. If TRUE (default) will detrend the data. If FALSE measurement values won't be detrended. If both, detrend_data and smooth_data are TRUE, the detrending will run over the smoothed data.}

\item{smooth_data}{Logical. If TRUE will smooth the measurement values using a moving average. Default = FALSE.}

\item{butterworth}{Logical. If TRUE (default) will apply a buttwerworth filter to the measurement values using a moving average. If FALSE measurement values won't be filtered.}

\item{f_low}{Frequency for the low pass filter. Default = 1/4.}

\item{f_high}{Frequency for the high pass filter. Default = 1/72.}

\item{order}{filter order. Default = 2.}

\item{binning_n}{A numeric indicating the amount of bins over which to run the smoothing average. Default = 4.}

\item{make_windows}{Logical. If TRUE, splits the timeseries into moving windows before analysis. Default = FALSE.}

\item{window_size_in_days}{a numeric indicating the width of the window size in day units.}

\item{window_step_in_days}{a numeric indicating the amount of day by which to move the window in day units.}

\item{big_data}{Logical. If TRUE, sets a multisession \code{future} plan for parallel processing. Default = FALSE.}

\item{ofac}{The Lomb-Scargle oversampling factor passed to [analyze_timeseries.lomb]. Default = 10.}

\item{lomb_pvalue}{The significance level used for the Lomb-Scargle analysis. Default = 0.01.}

\item{movavg}{Logical. If TRUE (default) will smooth the measurement values useing a moving average. If FALSE measurement values won't be smoothed.}

\item{datetime}{Optional if a data.frame is supplied. A POSIXct vector.}

\item{values}{Optional if a data.frame is supplied. A vector of values from a mesurement.}

\item{smoothing_n}{A numeric which indicated the amount of bins over which to run the smoothing average. Default = 4.}

\item{plot}{logical. If TRUE (default) plots the filtered data over the raw data. If FALSE, does not plot.}
}
\value{
A named list of data.frames containing the output of \code{butterworth_filter}, [find_gaps()],
[make_time_windows()], [analyze_timeseries.acf()], [analyze_timeseries.lomb()], and
[analyze_timeseries.cosinor()] for each measurement value.
}
\description{
Processes raw data in such a way that it can be directly inputted to the rhythm_analysis_by_window function.
}
\examples{
\dontrun{
processed_data <- process_timeseries.main(list_of_dfs, sampling_rate = "1 hour")
}

}
