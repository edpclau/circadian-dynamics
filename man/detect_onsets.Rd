% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onset_detection.R
\name{detect_onsets}
\alias{detect_onsets}
\title{Detect activity onsets}
\usage{
detect_onsets(
  df,
  sampling_rate,
  threshold = NULL,
  smooth_hours = 3,
  min_gap_hours = 16
)
}
\arguments{
\item{df}{A data.frame whose first column is a POSIXct `datetime` and whose last
column is the activity values.}

\item{sampling_rate}{Character sampling rate, e.g. `"30 minutes"` (used only to
size the default smoothing window).}

\item{threshold}{Activity threshold for an onset. If `NULL` (default) the mean of
the (smoothed) activity is used; pass a number, or `"median"` to use the median.}

\item{smooth_hours}{Width, in hours, of the centred moving-average pre-smoothing.
Default 1 h; set 0 to disable smoothing.}

\item{min_gap_hours}{Refractory gap: minimum hours between successive onsets, so a
single noisy cycle is not split into several. Default 8.}
}
\value{
A data.frame with `onset_index` (1, 2, ... in time order), `onset_datetime`,
  and `onset_hours` (hours from the first sample).
}
\description{
Finds activity onsets as rising threshold crossings of the activity
  trace: the points where (optionally smoothed) activity rises from below to above
  a threshold, with a refractory gap so each circadian cycle contributes at most
  one onset. Onsets are the standard phase marker for estimating the free-running
  period (see [estimate_tau()]); they are drawn in red on actograms.
}
